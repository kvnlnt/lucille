// Tabulous.prototype.exportToLucille = function(){

// 	return {
// 		root:this.settings.root.toUpperCase(),
// 		type:this.translateChordType(this.settings.type),
// 		caged:[0,this.voicings.length-1],
// 		voicings:this.voicings.map(function(voicing){ 
// 			return voicing.voicing.map(function(v,k){ 
// 				return {
// 					fret:v
// 				} 
// 			})
// 		})
// 	};

// };
